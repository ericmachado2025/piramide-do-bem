# Legacy SQL files

These files are historical references of early schema designs.
They do NOT reflect the current database schema in production.

Current schema is managed via Supabase MCP migrations applied directly
to production (see the migration history in information_schema).

DO NOT RUN these files — they are kept for archaeological reference only.
