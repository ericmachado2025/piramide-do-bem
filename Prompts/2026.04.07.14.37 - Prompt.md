# AUDITORIA COMPLETA + PROMPT DEFINITIVO — PIRÂMIDE DO BEM ESCOLAR
## Marcador temporal: 2026.04.07.12.00
## Este documento substitui todos os prompts anteriores. É a referência canônica.

---

# PARTE 1 — AUDITORIA DO BANCO DE DADOS

## 1.1 O que existe e está correto:
- ✅ 22 tabelas criadas (todas existem)
- ✅ 180.231 escolas carregadas do INEP
- ✅ 8 tribos na tabela `tribes`
- ✅ 161 personagens na tabela `characters`
- ✅ 9 action_types, 8 badges, 5 rewards
- ✅ 2 alunos cadastrados (Eric e Robson)
- ✅ Auth Supabase com Google OAuth configurado

## 1.2 O que está ERRADO no banco:

### ESCOLAS (schools):
- ❌ 180.230 de 180.231 escolas SEM latitude e longitude (só a FURG tem)
- ❌ 180.231 escolas SEM bairro (neighborhood = NULL)
- ❌ TODAS as 180.230 escolas com school_type = 'fundamental' (errado — há escolas de médio, EJA, técnico, superior)
- ❌ O INEP tem esses dados (coordenadas, bairro, tipo de ensino) — o Code não carregou

### TURMAS E MATRÍCULAS:
- ❌ Tabela `classrooms` VAZIA (0 registros) — o cadastro pede série/turma mas NÃO grava
- ❌ Tabela `student_enrollments` VAZIA — vínculo aluno-turma-ano não existe
- ❌ O campo `grade` e `section` não existem na tabela `students` — os dados se perdem

### AÇÕES:
- ❌ Apenas 9 action_types (deveria ter 16+). Faltam: Fui monitor, Acolhi aluno novo, Participei de projeto, Ajudei professor, Compartilhei material, Representei turma, Resgatei colega faltando, Ofereci ajuda proativamente, Limpei sala

### PERSONAGENS/COMUNIDADES (problema mais grave):
- ❌ O Code ACHATOU o modelo do Communities.sql
- ❌ O modelo original tem 4 níveis: Categories (8) → Types (77) → Communities (192) → Characters (4.394)
- ❌ O Code reduziu para 2 níveis: tribes (8) → characters (161)
- ❌ Perdeu 77 Types, 192 Communities e 4.233 Characters
- ❌ Perdeu os Levels (tiers) — cada Community tem 5 levels com ~5 characters por level
- ❌ Coluna `gender_filter` não existe — o Code usa `gender`
- ❌ Coluna `min_points` existe mas não reflete a relação correta com Levels

---

# PARTE 2 — AUDITORIA DO CÓDIGO

## 2.1 Violação DRY (Don't Repeat Yourself):
- ❌ 4 módulos de cadastro SEPARADOS e DIFERENTES:
  - `Cadastro.tsx` (aluno): 705 linhas — tem medidor de força de senha, seletor de data dia/mês/ano
  - `ProfessorCadastro.tsx`: 741 linhas — não tem medidor de força, campos diferentes
  - `ResponsavelCadastro.tsx`: 452 linhas — layout diferente, campos diferentes
  - `PatrocinadorCadastro.tsx`: 399 linhas — layout diferente
- ❌ Total: 2.297 linhas que deveriam ser ~500 (1 componente reutilizável com variações por role)

## 2.2 Segurança da senha:
- ❌ Todos os cadastros pedem apenas 6 caracteres (muito fraco)
- ❌ Deveria pedir: mín. 8 chars + 1 maiúscula + 1 minúscula + 1 número + 1 caractere especial
- ❌ Medidor de força existe apenas no cadastro do aluno — deveria ser em todos

## 2.3 Problemas de navegação e UX:
- ❌ Enter não avança para próximo passo (precisa clicar no botão)
- ❌ Não tem logout no Perfil
- ❌ Lista de beneficiados fechada na turma (deveria ser aberta a todos)
- ❌ school_type não filtra séries disponíveis
- ❌ Campo WhatsApp não existe no cadastro do aluno

## 2.4 Google OAuth:
- ✅ redirectTo usa `window.location.origin` (corrigido)
- ✅ Fallback para piramidedobem.com.br quando localhost
- ⚠️ O cadastro grava student mas NÃO grava classroom nem enrollment

## 2.5 Código morto / desnecessário:
- Vários arquivos com imports não utilizados (warnings no build)
- `CadastroPerfil.tsx` — parece redundante com `Cadastro.tsx`

---

# PARTE 3 — MODELO CORRETO DE COMUNIDADES

## O Communities.sql define esta hierarquia:

```
Categories (8)
  ├── Comic Books
  │   ├── Types: Marvel, DC, TMNT, Spawn, The Boys, Transformers, Walking Dead
  │   │   ├── Communities: X-Men, Avengers, Spider-Verse, Fantastic Four, ...
  │   │   │   ├── Characters (5 por level × 5 levels = 25 por community)
  │   │   │   │   filtrados por: Archetype (HERO, VILLAIN, ANTI_HERO, ENTITY)
  │   │   │   │                  Gender (MALE, FEMALE, OTHER)
  ├── Movies
  │   ├── Types: Harry Potter, Star Wars, LOTR, Avatar, Matrix, ...
  ├── Animes
  │   ├── Types: Dragon Ball, Naruto, One Piece, Bleach, Attack on Titan, ...
  ├── Sports
  │   ├── Types: Soccer, Basketball, Tennis, F1, MMA, Boxing, ...
  ├── Tabletop Games
  │   ├── Types: D&D, MTG, Pokemon TCG, Yu-Gi-Oh, Warhammer, ...
  ├── Music
  │   ├── Types: Rock, Pop, Hip Hop, Electronic, Metal, Classical, ...
  ├── TV Series
  │   ├── Types: Game of Thrones, Stranger Things, Breaking Bad, ...
  └── Generic
```

## Totais:
- 8 Categories
- 77 Types
- 192 Communities
- 4.394 Characters
- 960 Levels (5 por community)
- 5 Archetypes: HERO, VILLAIN, ANTI_HERO, ENTITY, NEUTRAL
- 3 Genders: MALE, FEMALE, OTHER

## Jornada correta de escolha:
```
1. Gênero do aluno → filtra personagens
2. Categoria (Comic Books, Movies, Animes, Sports...) → filtra Types
3. Type (Marvel, Dragon Ball, Harry Potter...) → filtra Communities
4. Community (X-Men, Avengers, Gryffindor, Jedi Order...) → filtra Characters
5. Archetype (Herói, Vilão, Anti-herói) → filtra Characters
6. Personagem inicial atribuído (Tier 1 do Level) → evolui com pontos
```

---

# PARTE 4 — PROMPT PARA O CLAUDE CODE

**INSTRUÇÕES: Leia este documento INTEIRO antes de começar. Execute na ordem indicada. NÃO crie arquivos duplicados — refatore os existentes.**

---

## BLOCO 1 — REFATORAR MODELO DE DADOS DO BANCO (Supabase)

### 1.1 Recriar tabelas de comunidades seguindo o Communities.sql:

Dropar as tabelas atuais `tribes` e `characters` e recriar seguindo a hierarquia:

```sql
-- Enum tables
CREATE TABLE IF NOT EXISTS community_categories (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  public_id UUID UNIQUE NOT NULL,
  name TEXT NOT NULL,
  description TEXT,
  slug TEXT UNIQUE NOT NULL,
  icon_class TEXT,
  color_hex VARCHAR(7),
  display_order INT NOT NULL,
  status TEXT DEFAULT 'ACTIVE',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS community_types (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  public_id UUID UNIQUE NOT NULL,
  category_id UUID REFERENCES community_categories(id) NOT NULL,
  name TEXT NOT NULL,
  description TEXT,
  slug TEXT UNIQUE NOT NULL,
  icon_class TEXT,
  color_hex VARCHAR(7),
  display_order INT NOT NULL,
  status TEXT DEFAULT 'ACTIVE',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS communities (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  public_id UUID UNIQUE NOT NULL,
  type_id UUID REFERENCES community_types(id) NOT NULL,
  name TEXT NOT NULL,
  description TEXT,
  slug TEXT UNIQUE NOT NULL,
  icon_class TEXT,
  color_hex VARCHAR(7),
  display_order INT NOT NULL,
  status TEXT DEFAULT 'ACTIVE',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS community_levels (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  public_id UUID UNIQUE NOT NULL,
  community_id UUID REFERENCES communities(id) NOT NULL,
  tier INT NOT NULL CHECK (tier BETWEEN 1 AND 5),
  name TEXT NOT NULL,
  min_points INT NOT NULL DEFAULT 0,
  display_order INT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(community_id, tier)
);

CREATE TABLE IF NOT EXISTS characters (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  public_id UUID UNIQUE NOT NULL,
  community_id UUID REFERENCES communities(id) NOT NULL,
  level_id UUID REFERENCES community_levels(id) NOT NULL,
  name TEXT NOT NULL,
  real_name TEXT,
  description TEXT,
  archetype TEXT NOT NULL CHECK (archetype IN ('HERO', 'VILLAIN', 'ANTI_HERO', 'ENTITY', 'NEUTRAL')),
  gender TEXT NOT NULL CHECK (gender IN ('MALE', 'FEMALE', 'OTHER')),
  display_order INT NOT NULL,
  status TEXT DEFAULT 'ACTIVE',
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 1.2 Carregar dados do Communities.sql:
Escrever script que parseia o Communities.sql (5087 linhas) e converte os INSERTs de SQL Server para PostgreSQL, preservando os PublicIds como UUIDs. Carregar TODOS os dados:
- 8 Categories
- 77 Types
- 192 Communities
- 4.394 Characters
- Criar 960 Levels (5 por community, com min_points: 0, 100, 300, 600, 1000)

### 1.3 Atualizar referências:
- `students.tribe_id` → renomear para `students.community_id` (referencia `communities`)
- `students.current_character_id` → manter (referencia `characters`)
- Migrar os 2 alunos existentes para os IDs corretos das novas tabelas

### 1.4 Corrigir escolas:
O INEP tem coordenadas e bairros nos microdados. O Code deve:
- Baixar novamente os microdados se necessário
- Fazer UPDATE nas 180.231 escolas com latitude, longitude, neighborhood e school_type correto
- O school_type deve refletir os níveis de ensino reais: 'infantil', 'fundamental_I', 'fundamental_II', 'medio', 'tecnico', 'eja', 'superior'
- Se uma escola tem múltiplos níveis (fundamental + médio), usar array ou texto separado por vírgula

### 1.5 Gravar turmas e matrículas:
No handleComplete do Cadastro:
1. Verificar se a classroom existe (school_id + grade + section)
2. Se não existir, criar na tabela `classrooms`
3. Criar enrollment em `student_enrollments` com academic_year = 2026

### 1.6 Adicionar action_types faltantes:
```sql
INSERT INTO action_types (name, points, icon, description, display_order) VALUES
('Fui monitor de grupo de estudo', 20, '🎓', 'Ajudei colegas em grupo de estudo', 10),
('Acolhi aluno novo na turma', 15, '🤝', 'Recebi e orientei aluno novo', 11),
('Participei de projeto coletivo', 20, '🏗️', 'Contribuí em projeto da turma', 12),
('Ajudei professor a organizar atividade', 15, '📋', 'Auxiliei o professor', 13),
('Compartilhei material de estudo', 10, '📤', 'Emprestei ou compartilhei material', 14),
('Representei a turma em evento', 30, '🎤', 'Representei colegas', 15),
('Resgatei colega que estava faltando', 30, '🔔', 'Fui atrás de colega ausente', 16),
('Ofereci ajuda proativamente', 20, '🙋', 'Me ofereci antes de ser pedido', 17),
('Limpei/organizei sala voluntariamente', 10, '🧹', 'Ação cívica voluntária', 18);
```

---

## BLOCO 2 — REFATORAR CADASTRO (DRY)

### 2.1 Criar componente único de cadastro:
Criar `src/components/CadastroUnificado.tsx` que recebe props:
- `role`: 'student' | 'teacher' | 'parent' | 'sponsor'
- `onComplete`: callback após cadastro

Campos comuns (todos os roles):
- Nome completo
- Email (com confirmação de código 6 dígitos se possível, senão validação básica)
- Senha (com medidor de força em TODOS) — regras: mín 8 chars + maiúscula + minúscula + número + caractere especial
- WhatsApp (opcional, com máscara)

Campos por role:
- Student: data nascimento, escola, série/turma, consentimento LGPD se menor
- Teacher: escola, disciplinas que leciona, série/turmas que atende
- Parent: CPF, celular (confirmação SMS), relação (pai/mãe/tutor), nome do filho
- Sponsor: CNPJ, endereço, categoria do negócio, tipo de benefício

### 2.2 Remover arquivos duplicados:
Após criar CadastroUnificado, refatorar:
- `Cadastro.tsx` → usa CadastroUnificado com role='student'
- `ProfessorCadastro.tsx` → usa CadastroUnificado com role='teacher'
- `ResponsavelCadastro.tsx` → usa CadastroUnificado com role='parent'
- `PatrocinadorCadastro.tsx` → usa CadastroUnificado com role='sponsor'

### 2.3 Validação de senha em todos:
```typescript
function validatePassword(password: string): { valid: boolean; errors: string[] } {
  const errors: string[] = []
  if (password.length < 8) errors.push('Mínimo 8 caracteres')
  if (!/[A-Z]/.test(password)) errors.push('1 letra maiúscula')
  if (!/[a-z]/.test(password)) errors.push('1 letra minúscula')
  if (!/[0-9]/.test(password)) errors.push('1 número')
  if (!/[!@#$%^&*()_+\-=\[\]{}|;':",.<>?/]/.test(password)) errors.push('1 caractere especial')
  return { valid: errors.length === 0, errors }
}
```

### 2.4 Enter para prosseguir:
Em TODOS os inputs de texto: `onKeyDown={(e) => e.key === 'Enter' && canAdvance() && handleNext()}`

---

## BLOCO 3 — JORNADA DE ESCOLHA DE TRIBO/PERSONAGEM

Reescrever EscolhaTribo.tsx e EscolhaPersonagem.tsx com a jornada correta:

### Passo 1: Gênero (já coletado no cadastro)
Usar o gênero informado no cadastro para filtrar personagens.

### Passo 2: Categoria
Tela com 8 cards (Comic Books, Movies, Animes, Sports, Tabletop Games, Music, TV Series, Generic).
Query: `community_categories` ORDER BY display_order

### Passo 3: Type (dentro da categoria)
Tela com cards dos Types da categoria escolhida.
Ex: se escolheu "Animes" → mostra Dragon Ball, Naruto, One Piece, Bleach, etc.
Query: `community_types` WHERE category_id = X

### Passo 4: Community
Tela com cards das Communities do Type.
Ex: se escolheu "Marvel" → mostra X-Men, Avengers, Spider-Verse, Fantastic Four, etc.
Query: `communities` WHERE type_id = X

### Passo 5: Archetype
Pergunta: "Qual tipo de personagem combina com você?"
- Herói (HERO) — "Eu gosto de fazer o bem"
- Anti-Herói (ANTI_HERO) — "Eu faço o bem do meu jeito"
- Vilão (VILLAIN) — "Eu gosto de desafiar"
Cards visuais com descrição.

### Passo 6: Personagem
Mostra os 5 personagens do Tier 1 (Level 1) da Community + Archetype + Gender escolhidos.
O aluno escolhe seu personagem inicial.
Mostra a progressão: "Conforme evolui, seu personagem sobe de nível dentro desta comunidade."

---

## BLOCO 4 — AJUSTES DE UX E FUNCIONALIDADES

### 4.1 WhatsApp no cadastro:
Adicionar campo opcional com máscara (XX) XXXXX-XXXX.
```sql
ALTER TABLE students ADD COLUMN IF NOT EXISTS whatsapp VARCHAR(15);
```

### 4.2 Lista de beneficiados na boa ação:
- ABERTA A TODOS os participantes (não restrita à turma)
- Campo autocomplete: busca por nome, email OU whatsapp
- Debounce 300ms, mín 2 caracteres
- Filtros opcionais: por tribo/community, por escola, por série

### 4.3 Séries filtradas por school_type:
Quando o aluno seleciona a escola, as séries disponíveis devem ser filtradas pelo school_type da escola:
- infantil → Pré I, Pré II
- fundamental_I → 1º ao 5º ano
- fundamental_II → 6º ao 9º ano
- medio → 1º ao 3º EM
- tecnico → 1º ao 4º Técnico
- eja → EJA Fundamental, EJA Médio
- superior → 1º ao 10º semestre

### 4.4 Logout no Perfil:
Botão "Sair" no final da tela de Perfil. Chama supabase.auth.signOut() + redireciona para /.

### 4.5 Botão "Recomendar":
Na Home, card para gerar link de indicação. Pontuação regressiva (5pts primeiras 5, 3pts 6-10, 1pt 11+).

### 4.6 Mapa com drill-down:
Nível 1: Brasil por estados (GeoJSON IBGE) com badge de total de usuários.
Nível 2: Clicar no estado → zoom em cidades com pins.
Nível 3: Clicar na cidade → bairros.
Nível 4: Clicar no bairro → escolas.
Tooltip em cada nível: escolas, alunos, professores, patrocinadores, monitores.
Mapa SEMPRE visível, mesmo com zero usuários (mostrar "0" em cada estado).

---

## BLOCO 5 — ORDEM DE EXECUÇÃO

1. BLOCO 1.4 — Corrigir escolas (lat/long, bairro, school_type) — SEM ISSO O MAPA NÃO FUNCIONA
2. BLOCO 1.1-1.3 — Recriar modelo de comunidades — SEM ISSO A JORNADA NÃO FUNCIONA
3. BLOCO 1.5 — Gravar turmas/matrículas — DADO CRÍTICO SENDO PERDIDO
4. BLOCO 1.6 — Adicionar action_types faltantes
5. BLOCO 2 — Refatorar cadastro DRY + senha forte + Enter
6. BLOCO 3 — Jornada de escolha Category → Type → Community → Archetype → Character
7. BLOCO 4.4 — Logout (funcionalidade básica ausente)
8. BLOCO 4.2 — Lista de beneficiados aberta + autocomplete
9. BLOCO 4.3 — Séries filtradas por school_type
10. BLOCO 4.6 — Mapa com drill-down
11. BLOCO 4.1 — WhatsApp
12. BLOCO 4.5 — Botão recomendar

Após CADA bloco: `npm run build` para verificar erros.
Após TODOS: `npm run build && git add -A && git commit -m "refatoração completa v1" && git push`

---

*Pirâmide do Bem Escolar — Auditoria + Prompt Definitivo v1 — 2026.04.07.12.00*
